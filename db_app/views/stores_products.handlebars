<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</head>
<body>

<p>Stores and Products Page</p>
<p>users submit one product and the app shows the qty sold at each store.
<br> (M:M Orders and Products, 1:M Stores and Orders)</p>


  <h2>Select Product</h2>                                      
  <div class="dropdown">
    <button class="btn btn-default dropdown-toggle" type="button" id="menu1" data-toggle="dropdown">Products
    <span class="caret"></span></button>
    <ul class="dropdown-menu" role="menu" aria-labelledby="menu1">
      {{#each products}}
      <li role="presentation"><a role="menuitem" tabindex="-1" href="#">{{product_id}}.  {{product_name}}</a></li>
      {{/each}}
    </ul>
  </div>

<br>


<!-- read/update/delete data -->
  <table class="table table-striped table-bordered" id="reportSP">
    <thead>
      <tr>
        <th>Store Id</th>
        <th>Store Name</th>
        <th>Product</th>
        <th>Quantity Sold</th>
      </tr>
    </thead>

    <tbody>
    {{#each stores}}
      <tr>
        <td>{{store_id}}</td>
        <td>{{store_name}}</td>
        <td>{{product_name}}</td>
        <td>{{Total}}</td>
      </tr>
    {{/each}}
  
    </tbody>
  </table>